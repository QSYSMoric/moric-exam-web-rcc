import type { Meta, StoryObj } from '@storybook/vue3';
import Contact from '@/components/company/add_contact/index.vue';

// 定义元数据
const meta: Meta<typeof Contact> = {
  title: 'Example/ContactForm',
  component: Contact,
  tags: ['autodocs'],
};

export default meta;
